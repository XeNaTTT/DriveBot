name: driveassistant_ar
description: "DriveAssistant AR - AR-inspired driving safety HUD foundation with mock data."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.2
  camera: ^0.12.0+1
  flutter_compass: ^0.8.1
  sensors_plus: ^7.0.0
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
